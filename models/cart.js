class Cart {
  constructor(quantity, itemPrice, itemBrand, sum) {
    this.quantity = quantity;
    this.itemPrice = itemPrice;
    this.itemBrand = itemBrand;
    this.sum = sum;
  }
}

export default Cart;
